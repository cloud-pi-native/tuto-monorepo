{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite --host --port 8080",
    "build": "tsc && vite build",
    "preview": "bunx --bun vite preview"
  },
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.2.0",
    "@types/bun": "^1.1.2"
  }
}
